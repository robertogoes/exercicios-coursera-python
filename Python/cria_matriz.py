def cria_matriz(num_linha, num_coluna, valor):
    matriz = []
    for i in range(num_linha):
        linha = []
        for j in range(num_coluna):
            linha.append(valor)
        print(linha, "\n")

    return 
